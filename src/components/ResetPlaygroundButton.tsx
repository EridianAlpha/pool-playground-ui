import { HStack, Text } from "@chakra-ui/react"

export default function ResetPlaygroundButton() {
    return (
        <HStack w={"100%"} gap={0} justifyContent={"center"}>
            <Text>Reset Playground</Text>
        </HStack>
    )
}
